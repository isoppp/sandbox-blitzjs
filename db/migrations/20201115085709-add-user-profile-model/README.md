# Migration `20201115085709-add-user-profile-model`

This migration has been generated by isoppp at 11/15/2020, 5:57:10 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "User" DROP COLUMN "name"

CREATE TABLE "Profile" (
"id" SERIAL,
    "name" TEXT,
    "imageUrl" TEXT,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "userId" INTEGER NOT NULL,

    PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "Profile_userId_unique" ON "Profile"("userId")

ALTER TABLE "Profile" ADD FOREIGN KEY("userId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201103033905-delete-project..20201115085709-add-user-profile-model
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgres"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -25,24 +25,24 @@
 // --------------------------------------
 model User {
-  id             Int           @default(autoincrement()) @id
+  id             Int           @id @default(autoincrement())
   createdAt      DateTime      @default(now())
   updatedAt      DateTime      @updatedAt
-  displayId      String        @default(cuid()) @unique
-  name           String?
+  displayId      String        @unique @default(cuid())
   email          String        @unique
   hashedPassword String?
+  profile        Profile?
   sessions       Session[]
   roles          Role[]
   posts          Post[]
   postLikes      LikePost[]
   postComments   PostComment[]
 }
 model Session {
-  id                 Int       @default(autoincrement()) @id
+  id                 Int       @id @default(autoincrement())
   createdAt          DateTime  @default(now())
   updatedAt          DateTime  @updatedAt
   expiresAt          DateTime?
   handle             String    @unique
@@ -54,18 +54,28 @@
   privateData        String?
 }
 model Role {
-  id    Int      @default(autoincrement()) @id
+  id    Int      @id @default(autoincrement())
   name  UserRole @unique
   users User[]
 }
+model Profile {
+  id        Int      @id @default(autoincrement())
+  name      String?
+  imageUrl  String?
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  user      User     @relation(fields: [userId], references: [id])
+  userId    Int
+}
+
 model Post {
-  id        Int           @default(autoincrement()) @id
+  id        Int           @id @default(autoincrement())
   createdAt DateTime      @default(now())
   updatedAt DateTime      @updatedAt
-  slug      String        @default(cuid()) @unique
+  slug      String        @unique @default(cuid())
   title     String
   content   String
   status    PostStatus    @default(POST_STATUS_DRAFT)
   author    User          @relation(fields: [authorId], references: [id])
@@ -76,9 +86,9 @@
   @@index([status])
 }
 model LikePost {
-  id        Int      @default(autoincrement()) @id
+  id        Int      @id @default(autoincrement())
   createdAt DateTime @default(now())
   post      Post     @relation(fields: [postId], references: [id])
   postId    Int
   user      User     @relation(fields: [userId], references: [id])
@@ -87,9 +97,9 @@
   @@unique([postId, userId])
 }
 model PostComment {
-  id        Int           @default(autoincrement()) @id
+  id        Int           @id @default(autoincrement())
   createdAt DateTime      @default(now())
   updatedAt DateTime      @updatedAt
   content   String
   post      Post          @relation(fields: [postId], references: [id])
```


